# ──────────────────────────────────────────────
# Nexora Frontend (Vite) — same as frontend/.env.example
# Vite loads .env from the project root by default.
# ──────────────────────────────────────────────

# API (REQUIRED NOW) — public URL only
VITE_API_URL=http://localhost:5000/api/v1

# Production:
# VITE_API_URL=https://your-api.up.railway.app/api/v1

# Do NOT put JWT, Google Client Secret, Gemini, Cloudinary, or DB secrets here.
